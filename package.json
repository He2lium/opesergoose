{
  "name": "opesergoose",
  "version": "1.0.26",
  "description": "Mongoose plugin for OpenSearch",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "tsc && npm version patch",
    "publish": "npm publish"
  },
  "files": [
    "dist"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.7.0",
    "@types/omit-deep": "^0.3.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@opensearch-project/opensearch": "^2.4.0",
    "mongoose": "^7.6.2",
    "omit-deep": "^0.3.0"
  }
}
